package interfaces;

public interface Figura {

    //Usaremos los mismos ejercicios de clases abstractas
    //Eliminamos todo y solo dejamos el metodo abstracto
    //Cambiamos la palabra extends por interface

    //aca no es necesario especificar la palabra abstract en los metodos
    public double calcularArea();
}

